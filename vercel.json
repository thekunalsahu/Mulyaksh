{
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 .vercel_flutter && export PATH=\"$PATH:$(pwd)/.vercel_flutter/bin\" && flutter config --enable-web && flutter pub get && flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
