# Mulyaksh

A Flutter coming soon landing page for Mulyaksh.

## Getting Started

Run the app locally:

```bash
flutter pub get
flutter run -d web-server
```

Build for web:

```bash
flutter build web --release
```
